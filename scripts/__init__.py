# Empty marker so scripts/ can be imported as a package — used by
# scripts/test_ipadapter.py to reuse the SOFT_SCENES/FULL_SCENES pools and
# the small set of staging helpers from scripts/render_set.py without
# duplicating them.
